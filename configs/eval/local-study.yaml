name: Offline evaluator smoke comparison
baseline: base
axis: tokens
budget: 128
tolerance: 0.0
allow_changes:
- model.params.dropout
variants:
- name: base
  results:
  - "@/runs/evaluations/eval-local-base/*.json"
- name: variant
  results:
  - "@/runs/evaluations/eval-local-variant/*.json"
